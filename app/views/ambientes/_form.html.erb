<div class="row">
  <div class="col-lg-offset-2 col-lg-7">
    <%= form_for(@ambiente, :html=>{:class=>"form-horizontal", :role=>"form"}) do |f| %>
      <!--
      <% if @ambiente.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@ambiente.errors.count, "error") %> prohibited this ambiente from being saved:</h2>

          <ul>
          <% @ambiente.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>
     -->
      <div class="form-group">
        <%= f.label :ambiente, :class=>"control-label col-lg-3"  %>
        <div class=" col-lg-9">
          <%= f.text_field :ambiente, :class=>"form-control", :placeholder=>"Nombre del Ambiente" %>
        </div>
      </div>
      <div class="form-group">
        <%= f.label :sistema_domotico_id, :class=>"control-label col-lg-3"  %>
        <div class=" col-lg-9">
          <%= f.text_field :sistema_domotico_id, :class=>"form-control", :placeholder=>"Sistema Domotico" %>
        </div>
      </div>
      <div class="form-group">
        <div class="col-lg-offset-3 col-lg-2">
          <%= f.submit :class=>"btn btn-primary" %>
        </div>
      </div>
    <% end %>
  </div>
</div>